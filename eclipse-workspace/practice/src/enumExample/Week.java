package enumExample;

/*enumeration representing a group of named constants in Java programming language
 * enum can declare pre-defined constants which a Class or Interface can't 
 * enumerated natural types like the planets, days of the week, months of the year, color, directions etc
 * each enum constant represents an object of enum type
 * enum object can pass as an argument to switch statement
 * 
 */
public enum Week {
	Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday, NoDay

}
